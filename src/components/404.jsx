import React from 'react';

var NotFound = React.createClass({
    render: function() {
      return (
        <div>404 Page error</div>
      );
    }
// resetTimers

// tick - countdown

// start

//

});

module.exports = NotFound;
